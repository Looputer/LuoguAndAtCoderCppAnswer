#include <iostream>
#define int long long
using namespace std;

int r, c, k;
int val[3005][3005];
int dp[3005][3005][4];

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin >> r >> c >> k;
    for (int i = 1; i <= k; i++) {
        int a, b, v;
        cin >> a >> b >> v;
    }
}